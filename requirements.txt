# LeadRadar v3 — Webhook Edition
# No gspread or google-auth needed anymore.

# HTTP client — Places API calls + webhook POSTs
requests>=2.31.0

# Web framework + server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Data validation for FastAPI request schemas
pydantic>=2.0.0
